const canvas = document.getElementById("gameArea");
const ctx = canvas.getContext('2d'); // ctx means context. The context of our drawings are two dimensional

ctx.fillStyle = 'black';
ctx.fillRect(0,0, canvas.width, canvas.height);



